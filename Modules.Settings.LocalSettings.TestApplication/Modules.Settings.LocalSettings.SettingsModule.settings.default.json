{
  // This is a test integer which shoulg be replaced by the testInt property in settings.json
  // when SettingsModule parses each settings file in order starting with settings.default.json
  "testInt": 1
  /*
    Comments are removed from JSON configuration files by SettingModule. This enables the module
    creator to be verbose about their module's configuration settings.
   */
}
